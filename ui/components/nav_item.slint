export component NavItem inherits Rectangle {
  in property <string> text;
  in property <bool> active;
  callback clicked();

  border-radius: 4px;
  background: root.active ? #7678ed : #383850;
  height: 40px;

  Text {
    text: root.text;
    color: root.active ? white : #eeeeee;
    font-weight: root.active ? 700 : 400;
  }

  touch_area := TouchArea {
    clicked => { root.clicked() }
  }

  states [
    hover when touch_area.has-hover: {
      background: root.active ? #7678ed : #4c4c68;
      in { animate background { duration: 150ms; } }
      out { animate background { duration: 150ms; } }
    }
  ]
}
