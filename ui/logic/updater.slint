// SPDX-FileCopyrightText: 2025 Spencer
// SPDX-License-Identifier: AGPL-3.0-only

export global UpdaterLogic {
  in property <string> current_version;
  in property <string> new_version;
  in property <string> changelog;
  in-out property <bool> downloading;
  callback download_update();
  callback skip_update();
}
